import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import Button from './Button';
import { AiFillAudio } from "react-icons/ai";


<Meta title='MDX/Button' component={Button} />

# Button

With `MDX` we can define a story for right in the middle of our Markdown documentation.
~~assd fsaff sfsf fsf s fsaf~~

<Canvas>
  <Story name='button'>
      <Button       text="Button"
    textClassName=""
    className=""
    Icon={AiFillAudio}
    IconClassName=""
    SpinnerClassName="" 
        />
  </Story>
</Canvas>
